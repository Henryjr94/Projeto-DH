var express = require('express');
var router = express.Router();
const control = require('../controller/editControl')

// router.put('/cliente',control.listCliente)
// router.put('/endereco',control.listEndereco)
// router.put('/livro',control.listLivro)

module.exports = router;